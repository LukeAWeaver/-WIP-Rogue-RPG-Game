using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
      //  gameObject.GetComponent<RectTransform>().anchoredPosition = new Vector2(gameObject.GetComponent<RectTransform>().position.x + Random.Range(-5f, 5f), gameObject.GetComponent<RectTransform>().position.y);

public class randomPosition : MonoBehaviour {

    void Start()
    {

    }
    private void Awake()
    {
    }
    // Update is called once per frame
    void Update () {
		
	}
}
